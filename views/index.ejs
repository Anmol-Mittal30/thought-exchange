<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>All Posts</title>
    <link rel="stylesheet" href="/style.css" />
  </head>
  <body>
    <h1>Quora Posts</h1>
    <% for(post of posts){ %>
    <div class="post">
      <h3 class="user">Username: @<%= post.username %></h3>
      <h4 class="content">Content: <%= post.content %></h4>
      <h3><%=post.title%></h3>
      <br />
        <div class="post-actions">
           <a href="http://localhost:8080/posts/<%= post.id %>" class="button-see">See this post</a>
      <a href="http://localhost:8080/posts/<%= post.id %>/edit" class="button-edit">Edit this post</a> 
      <form method="post" action="/posts/<%=post.id%>?_method=DELETE">
        <button  class="button-delete">Delete Post</button>
      </form>
        </div>
    </div>
    <% } %>
    <br />
    <br />
    <a href="http://localhost:8080/posts/new">Create new Post</a>
  </body>
</html>
